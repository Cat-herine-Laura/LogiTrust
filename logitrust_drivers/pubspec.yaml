name: logitrust_drivers
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.2.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.1.2
  firebase_database: ^11.0.3
  fluttertoast: ^8.2.6
  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  flutter_polyline_points: ^2.1.0
  location: ^7.0.0
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
    
